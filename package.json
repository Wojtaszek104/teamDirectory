{
  "name": "team-directory",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
